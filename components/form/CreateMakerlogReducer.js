const initialState = {
  makerlog: {
    id: new Date().getMilliseconds(),
    createdOn: new Date(),
    title,
    content,
    tags: [],
    topics: [],
    shareSettings,
  },
  makerlogs: [],
};

const makerlogReducer = (prevState, action) => {
  switch (action) {
  }
};
